# Keep scripts LF on every platform: the agent hashes its own file for auto-update
# and CRLF would also break the bash installers' shebang lines.
* text=auto
*.py text eol=lf
*.sh text eol=lf
*.service text eol=lf
*.ps1 text eol=crlf
*.png binary
