# Avalon Monitor agent configuration (KEY=VALUE). Environment variables override.

# Hub address over Tailscale (MagicDNS name or 100.x.y.z IP) - never the public domain.
AVM_SERVER_URL=http://avalon:8787
# Token from `manage.py add-host <name>` on AVALON.
AVM_TOKEN=

AVM_INTERVAL=5
#AVM_HOSTNAME=
#AVM_TAGS=gpu,training

# Top-N processes by CPU to report (0 disables).
AVM_PROCESSES=8

# Liveness checks (optional) - shown as badges on the host card.
#   process names or command-line substrings:
#AVM_WATCH_PROCESSES=queue_runner.sh,ollama
#   heartbeat files with max age in seconds (path:seconds):
#AVM_WATCH_FILES=/home/user/.queue/heartbeat:120
#   TCP ports that should be open:
#AVM_WATCH_TCP=127.0.0.1:11434
#   paths that must be mounted (removable / network volumes a job depends on):
#AVM_WATCH_MOUNTS=/run/media/user/Data
#   small text files whose first line is shown on the card (e.g. "current job"):
#AVM_WATCH_CONTENT=/srv/queue/current.txt
#   globs that must match nothing - a crashed run announces itself with a marker file:
#AVM_WATCH_MARKERS=/srv/queue/*.failed
#   files that should never be empty - an empty queue is a warning, not success:
#AVM_WATCH_NONEMPTY=/srv/queue/queue.txt

# Windows only: LibreHardwareMonitor's web server for CPU temp / AMD-Intel GPU data.
#AVM_LHM_URL=http://localhost:8085/data.json

# Replace this script with the hub's copy when the hub is upgraded (verified by hash), then respawn.
AVM_AUTO_UPDATE=true

AVM_LOG_LEVEL=info
